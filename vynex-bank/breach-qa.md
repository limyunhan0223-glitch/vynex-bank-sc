# Scene 04 verification

The existing form discards demo identity, password and Remember me immediately. Its existing 450ms processing pause now reads Verifying... in blue. simulationSubmitted then enables the isolated breach sequence: drain at 450ms; impact/triangles at 850ms; reveal at 1200ms; settled state around 2000ms. GSAP context owns cleanup. No camera writes were added.

Nine bevelled extruded glass/ruby triangles use three instanced draws (rim, smoked core, extruded exclamation). Shared geometry/materials, no shadows, transmission passes, new Canvas or bloom. Entrance uses elapsed time; forensic dimming uses delta-time damping. Updates stop when settled. Reduced-motion entrance skips depth/scale/rotation travel and retains fade; CSS skips environmental pulse and evidence travel.

Desktop 1920x1080 and mobile 390x844 were inspected. Corrected a focus-induced hidden-host scroll, shield hue, privacy-line clipping and the longest forensic clue layout. Tested all three clues including keyboard arrow navigation. Protection request stops in Scene 04. Demo fields empty after submission; browser data-request counter remained zero. No form data is stored, transmitted or logged. Audio is not used.

18 automated checks cover existing transitions, immediate data clearing, narrative reset, reduced-motion and triangle entrance bounds. Existing source hash comparison limits changes to main.tsx (CSS import), VynexScene.tsx (new hidden-until-triggered mesh group), and DeceptionScreen.tsx (submission integration). No earlier scene styles, models, camera/motion code or assets changed.

Measured settled desktop diagnostic: approximately 56 FPS, 41 draw calls during browser inspection. This is a local observation, not a universal 60 FPS guarantee. Final fresh-load console/build verification and captures recorded in task history. Development hot refresh temporarily reported hook-order errors after removing an unused hook; a clean reload is required after that edit.
